= simple_form_for([@note, @note.questions.build], remote: true) do |f|
  = f.input :content, as: :summernote, label: false, required: "true"
  = f.button :submit, value: "Ask Question", class: "btn-std btn-sm pull-right my-2"
