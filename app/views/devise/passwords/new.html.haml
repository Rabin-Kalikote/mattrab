
.this-wrap
  .container
    .welcome.col-md-5
      %h2.title Forgot Password?
      %p.messege.d-sm-none.d-md-block
        Nothing to worry, we can help you recover it.
        %br Mattrab always wants to help you!
      - if controller_name != 'sessions'
        %p.if.d-sm-none.d-md-block Or remembered? Sign in here.
        = link_to "Sign In", new_session_path(resource_name), class: "btn-std btn-else d-sm-none d-md-block"
    .form-wrap.login.col-md-7
      .this-form
        = simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
          = f.error_notification
          .form-inputs
            .form-label-group.mb-3
              = f.input :email, required: true, label: false, placeholder: "recoveryemail@mail.com", autofocus: true, input_html: { autocomplete: "email", class: 'form-control' }
          .form-actions.pt-3
            = f.button :submit, "Send Me Instructions", class: 'btn-std btn-block btn-submit'


-# <h2>Forgot your password?</h2>
-#
-# <%= simple_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
-#   <%= f.error_notification %>
-#
-#   <div class="form-inputs">
-#     <%= f.input :email,
-#                 required: true,
-#                 autofocus: true,
-#                 input_html: { autocomplete: "email" } %>
-#   </div>
-#
-#   <div class="form-actions">
-#     <%= f.button :submit, "Send me reset password instructions" %>
-#   </div>
-# <% end %>
-#
-# <%= render "devise/shared/links" %>
